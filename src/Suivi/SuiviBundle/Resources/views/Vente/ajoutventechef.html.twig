{% extends 'SuiviVenteBundle::layout.html.twig' %}
{% block body %}
  
    <title>Suivi Vente | Dashboard</title>

      {% include 'SuiviVenteBundle::header.html.twig' %}
      {% include 'SuiviVenteBundle::colonne-gauche.html.twig' %}

  <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
          <h1>
          Ajout ventes
            
          </h1>
          <ol class="breadcrumb">
            <li><a href="#"><i class="glyphicon glyphicon-hand-right"></i> Accueil</a></li>
            <li class="active">Ventes</li>
            <li class="active">Ajouter vente</li>
          </ol>
        </section>
	<section class="content">
          <div class="row">
              
            <div class="col-xs-12">
      
            <div class="box-body bloc-l">
            <div class="tabs_cont" id="tabs-1">
             {% for a in categoryNames %}
                <h2> {{ a.nom }}</h2>
             {% for offre in a.offre %} 
                 <a class="cli" href="{{ offre.id }}">  {{ offre.nom }}</a>
             {% endfor %}    
             {% endfor %}    

            </div><!--/col-12-->

            </div>
             <div class="box-body bloc-r">
              {% if message %}  
                <div class="alert alert-success">
                <a href="#" class="close" data-dismiss="alert">&times;</a>
                {{ message }}

                  </div>
                {% endif %}
             <form method="post" enctype="multipart/form-data" class="form-ajout">
                   
                    <div class="control-group rech">
                        {{ form_start(form) }}
                         <div class="form-group">
                        {{ form_row(form.date,{'attr':{'class':'form-control'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.offres,{'attr':{'class':'form-control'}}) }}
                        </div>
                        <div class="form-group">
                        {{ form_row(form.quantite,{'attr':{'class':'form-control'}}) }}
                        </div>
                    </div>
                        <div class="clear_both"></div>
                      <div class="btn btn3">
                            <button type="submit" class="btn btn-primary">Ajouter</button>
                            <button type="reset" value="Rénisialiser" class="btn btn-annuler">Annuler</button>
                      </div>
                    {{ form_end(form) }}
            </form>
                </div>
            <div class="clear_both"></div>
                  
                
 
           
              
      
        
        <!----debut---------!---->

    


      </div><!-- /#myCarousel -->
  </div><!-- /.box-body -->
              </div><!-- /.box -->
            </div><!-- /.col -->
          </div><!-- /.row -->
	</section>
	</div>



{% include 'SuiviVenteBundle::footer.html.twig' %}

    </div><!-- ./wrapper -->


    {% endblock %}